#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,k; cin >> n >> k;
    int mx=0;
    while(n--)
    {
        int a; cin >> a; mx = max(mx,a);
        while(a--)
        {
            int t; cin >> t;
        }
    }
    cout << mx/k;
}
