#include<bits/stdc++.h>
using namespace std;
using ll = long long;
int main()
{
    ll n,mn=LONG_MAX,sum=0;
    cin >> n; vector<ll> v(n);
    for(int i=0;i<n;++i)
    {
        cin >> v[i];
     //   mn = min(mn,a[i]);
    }
    sort(v.begin(),v.end());

}
