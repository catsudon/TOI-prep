#include<bits/stdc++.h>
#define x first
#define y second
using namespace std;
using ll = long long;
using pll = pair<ll,ll>;
int const hs = 5e6+11;
pair<int,int> a[1509],b[1509],c[1509],d[1509];
int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    pii dem;
    cin >> dem.x >> dem.y;
    int n; cin >> n;
    for(int i=1;i<=n;++i) cin >> a[i].x >> a[i].y;
    for(int i=1;i<=n;++i) cin >> b[i].x >> b[i].y;
    for(int i=1;i<=n;++i) cin >> c[i].x >> c[i].y;
    for(int i=1;i<=n;++i) cin >> d[i].x >> d[i].y;


}
